[project]
name = "00981a-tracker"
version = "0.1.0"
description = "Daily tracker for 00981A ETF holdings, auto-updated via GitHub Actions"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "scrapling[fetchers]>=0.4.8",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
